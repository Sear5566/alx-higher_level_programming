#!/usr/bin/python3
def weight_average(my_list=[]):
    weight = 0
    score_weight = 0
    if my_list:
        for tuples in my_list:
            nume += (tuples[0] * tuples[1])
            den += tuples[1]
        return (score_weight / weight)
    return (0)
